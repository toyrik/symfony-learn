# Описание

Контейниризированное приложение на symfony 

Для запуска контейнеров

```bash
# С пересборкой контейнеров
make init

# Без пересборки контейнеров
make up
```

Для останова контейнеров

```bash
make down
```

Если возникли ошибки с доступом к логам и кешу:

```bash
sudo chmod -R 777 var/cache var/logs var/sessions
```
